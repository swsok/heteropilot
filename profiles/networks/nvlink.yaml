# Intra-node NVLink link class used by ScenarioLab's random cluster generator.
#
# PROVENANCE (absolute rule 3): every number below is source: placeholder.
# The values are copied verbatim from the NVLink pair in
# examples/clusters/heterogeneous-lab.yaml, which were themselves never
# measured. Nothing here may be cited as a measurement.
link_id: nvlink
type: NVLINK
scope: intranode
bandwidth_gbps: 900
latency_ns: 1000
energy_per_bit_pj: 1.0
source: placeholder
