# HeteroPilot 작업지시서 — 특허 2 정규 명세서용 검증 실험 (p2-evidence)

> 특허 2("동작점별 예측 오차 검증 영역을 이용한 LLM 서빙 배치 후보의 실행가능성 판정", 직무발명 신고서 v5) 정규 명세서의 실시예·효과 절을 채우기 위한 실험. 신고서 §6·§8(3)이 "확인 후 확정한다 / 별도 평가한다"로 남긴 항목이 그대로 이 지시서의 STEP이다.
> 대상 저장소: `github.com/swsok/heteropilot` · 작성일: 2026-09-16 · 기준 `main` = `201f99e` **+ D70(PR #87, uq-B+ C0)**
>
> **개정 rev 2 (2026-09-16).** 초안의 V0 전제 두 개가 D70에 의해 반박되어 §1 표와 V0을 다시 썼다. (1) `rngd_card_edf.yaml`의 76점 `−18.0`은 D22의 `(57.1−48.41)/48.41`을 부호만 바꾼 값이 **아니라** 엔벨로프 자신의 실측·예측 쌍 `(43.2 − 52.7)/52.7`이며 파일 규약상 옳다. 정정 대상이 아니다. (2) **57.1 ms는 측정값이 아니다** — `48.41 × 1.18`이라는 한 계산식의 반복이고, 일곱 문서에 퍼져 있다. 18 % 뒤의 유일한 측정은 동시성 76(c64·c128 실측의 보간)에서의 **실측 52.7 ms / 예측 43.2 ms** 쌍이다. 잘못은 저장된 값이 아니라 **곱셈**(코드 규약 `e`를 `sim×(1−e)`로 쓴 것)에 있었고, 보정된 식 `m = −e/(1+e)`로는 D22 승자의 보정 TPOT가 57.1이 아니라 **59.04 ms**다. 따라서 V0은 "집계 기준 확정"이 아니라 **"57.1의 출처 추적과 신고서 정정"**이 된다. 신고서 v5의 §3·§5.6·§6·§8(3)·도 4가 48.41/57.1을 예측/실측 쌍으로 인용하고 있으므로 **제출 전에 고쳐야 한다**(이 지시서와 함께 전달되는 v5 검토수정본 2에 반영).
> 도구: Claude Code. **V0·V1·V4는 CPU 노드**, **V2·V3는 A40 노드 각 반나절**(RNGD 노드 불필요).
> 선행 문서: `docs/uncertainty_planner.md`, `docs/a40_live_deploy_loop.md`, `docs/rps_aware_planning_design.md` §5, `profiles/calibration/a40.accuracy.yaml`, `rngd_card_edf.yaml`, `experiments/uncertainty/results/ea1_margin_modes.md`, `docs/deviations.md` D19·D22·D29·D32·D33
> 관련 지시서: `WORK_ORDER_uq_stage_b_plus.md`(C0의 잔차 분모 정리는 이 지시서 V0과 같은 일이다 — 먼저 끝난 쪽을 다른 쪽이 참조)

---

## 0. 사용법과 규칙

1. V0 → V1 → (V2, V3 병행 가능) → V4. V2·V3는 서버 일정에 맞춰 순서를 바꿔도 된다.
2. 한 STEP = 한 브랜치 = 한 PR. 브랜치 `exp/p2ev-v<N>-<짧은이름>`.
3. 이 지시서의 산출물은 **특허 명세서에 인용될 수치**다. 따라서 (a) 모든 수치는 결과 md에 원자료 경로와 함께 남기고, (b) 신고서 v5의 정의를 그대로 쓴다 — 잔차 `r = (실측 − 예측)/예측`(양수 = 실제 지연이 더 김), 마진은 비음수, 조회 좌표는 **예측 서비스 동시성**, 서비스 동시성은 관측 구간 내 요청 체류시간 합 / 구간 길이. 코드의 내부 규약(`e = (sim − measured)/measured`)과 다르면 결과 md에 **양쪽을 병기**하고 명세서에는 신고서 정의만 쓴다. 두 규약의 환산은 `r = −e/(1+e)`이며, 마진을 곱할 때 `sim×(1−e)`를 쓰면 실측을 복원하지 못한다(D70).
4. 결과가 가설과 다르면 그대로 기록한다. 이 지시서의 실험 중 하나가 발명에 불리하게 나와도 그것이 명세서의 한계 서술이 된다.
5. 절대 규칙 A1~A4(`WORK_ORDER_uncertainty_planner.md`)는 그대로 적용된다. 특히 **A3**: 실측 파일은 새 파일로 쓰고 기존 파일을 덮어쓰지 않는다.

---

## 1. 무엇이 비어 있는가 — 신고서 v5 대비

| 신고서 위치 | 현재 상태 | 비어 있는 것 | STEP |
|---|---|---|---|
| §3·§5.6·§6·§8(3)·도 4 "48.41 ms(예측) / 57.1 ms(실측)" | **57.1은 실측이 아니다**(D70): `48.41×1.18` 계산값. 실측은 동시성 76의 52.7 ms(c64·c128 보간), 예측 43.2 ms | 신고서·명세서에서 57.1을 제거하고 52.7/43.2 쌍과 보정 TPOT 59.0 ms로 교체. 52.7/43.2의 집계 기준 확정 | V0 |
| §6 "잔차 분모 … 예측값 분모로 통일" | 76점 `−18.0`은 파일 규약 `(sim−meas)/meas`로 **옳음**(D70). 불일치는 값이 아니라 마진 곱셈에 있었음 | 9점 각각의 출처와 두 규약 값(파일 규약 e, 신고서 규약 r = −e/(1+e)) 병기 표. 76점은 정정하지 않음 | V0 |
| §5.4 검증 영역 2단계(보정점/검증점, 초과 비율) | 절차만 서술. 실측 9점에 적용된 적 없음 | 9점에 절차를 적용해 등록되는 구간 표 | V1 |
| §5.2·§5.3·청구항 2 "예측 서비스 동시성 기준 조회" | 9점 모두 도착률 일치 비교라 예측 L ≈ 실측 L. 두 좌표가 갈라지는 사례 없음 | 개루프(도착률 구동) 실측에서 예측 L과 실측 L의 차이, 그리고 어느 좌표로 조회해야 판정이 맞는지 | V2 |
| §6 "실제 SLO 위반 후보의 통과 비율, 실제 만족 후보의 제외 비율, 보류 비율" | E-A1은 규칙별 통과 수(70/10/50, 보류 30)만 있음. 통과 후보가 실제로 SLO를 만족했는지 **미측정** | 대표 후보를 실제 배포해 네 규칙의 판정과 실측 비교 | V3 |
| 도 2(설명용 4점) | 가상 값 | 실측 9점 곡선(부호 전환, 마진 0 구간, 검증 영역) | V4 |
| §5.7 동시성 구간 I_c 변형 | 미검증 | 정규 명세서에서도 변형 실시예로 둠 — **이 지시서 범위 밖** | — |

---

## 2. STEP

### STEP V0 — 57.1의 출처 추적, 규약 병기표, 신고서 정정 (CPU, 0.5일)

**전제(D70).** 76점 `−18.0`은 정정 대상이 아니다. 57.1 ms는 측정값이 아니다. 마진 곱셈은 `m = −e/(1+e)`로 보정되었다(PR #87 결과를 **조사 필요**로 확인: `planner/optimizer/margin.py`가 현재 어느 식을 쓰는지, E-A1·E6 회귀가 어떻게 됐는지 D70 본문에서 읽어 기록).

**할 일**
- **57.1의 출처 추적.** D70이 나열한 일곱 문서(`PROJECT_REPORT.md`, `rps_aware_planning_design.md`, `uncertainty_planner.md`, `patent_future_ideas.md`, `e5_self_rejection.md`, `rngd_card_edf.yaml` note, 그 외)에서 57.1의 출현을 모두 찾아 표로 만들고, 각각이 "계산값임을 표기 / 59.04로 교체 / 삭제" 중 어느 처리를 받았는지(또는 받아야 하는지) 적는다. 레포 문서 수정은 D70/PR #87이 이미 했으면 참조만, 남은 것이 있으면 이 STEP에서 한다. **신고서·명세서 쪽 정정은 아래 별항.**
- **동시성 76 실측·예측 쌍의 집계 기준 확정.** `experiments/results/rngd_concurrency_envelope.md`의 52.7 ms(c64 44.54 ms·c128 67.88 ms 실측의 76 보간)와 시뮬레이터 43.2 ms가 각각 어떤 집계(요청 평균 TPOT의 평균? p50? p95?)인지 원자료(`outputs/rngd_envelope/edf/real_c64.json`, `real_c128.json`, 대응 sim CSV)에서 확정한다. 다르면 같은 기준으로 재계산해 병기. 이 쌍이 신고서 §3·§6의 유일한 고부하 실측 근거가 된다.
- **규약 병기표.** RNGD-CARD 9점과 A40 3점 각각에 대해 (i) 출처(도착률 일치 300요청 비교 / 프로파일 적합점 / 엔벨로프 보간), (ii) 파일 규약 `e = (sim−meas)/meas`, (iii) 신고서 규약 `r = (meas−sim)/sim = −e/(1+e)`, (iv) 표본 수. 76점: e = −18.0 %, r = +22.0 %. 16.6점: e = −3.1 %, r = +3.2 %. 표는 `experiments/p2_evidence/v0_convert.py`로 생성(손계산 금지).
- **신고서 v5 정정 목록 작성**(실제 수정은 검토수정본으로 사용자에게 전달): §3 "예측 48.41 / 실측 57.1 / 17.95 %" → "예측 48.41(추천 후보, 미배포) / 동시성 76 실측 52.7 대 예측 43.2 → r ≈ 22.0 % → 보정 59.0 ms"; §5.6 후보 B 마진 18 % → 22 %, 보정 57.1 → 59.0, 전역 마진 비교도 22 %; §6 효과 첫 항목과 [수행한 측정과 판정 비교] 둘째 단락; §8(3); 도 2·도 4 수치. E-A1의 "(b) 전역 마진 18 %"는 **실험에서 실제로 쓴 규칙**이므로 그대로 두되 "종전 기록의 18 %"로 표기.

**산출물** `experiments/p2_evidence/results/v0_source_reconciliation.md` — 57.1 출현·처리 표, 52.7/43.2 집계 기준 결론, 12점 규약 병기표, 신고서 정정 목록.

**테스트** 없음(문서). 환산표는 스크립트 생성.

### STEP V1 — 검증 영역 2단계 절차를 실측 9점에 적용 (CPU, 1일)

**할 일** 신고서 §5.4의 절차를 코드로 구현해 RNGD-CARD 9점과 A40 3점에 적용한다.
- 입력: V0의 표(예측 동시성 좌표, 신고서 규약 잔차, 표본 수 — 300요청 비교는 n=300, 적합점·보간점은 n을 원자료에서).
- **보정점/검증점 배치** 두 가지를 모두 계산한다. (A) 전부 보정점(신고서 §5.6의 설명용 예와 같은 배치) — 이 경우 내부 검증점이 없어 어떤 구간도 등록되지 않는다는 것을 **확인 결과로** 기록. (B) §5.7의 변형: 양 끝(1.02, 76)을 보정점으로, 내부 7점을 검증점으로. 추가로 (C) 인접 쌍마다 한 점씩 건너 보정/검증을 교대(leave-one-out 배치).
- 1단계 조건: 인접 동시성 비율 ≤ 2, 표본 수 ≥ n_min(값을 정하고 근거를 적음; 300요청 비교는 통과, 보간점 76은 원자료 n을 확인), 동작 모드 동일(RNGD-CARD의 포화 구간 여부 — D32의 "served 44 근처 포화" 기록 참조).
- 2단계 조건: 검증점에서 `실측 지연 > 보정 지연(예측×(1+m))`인 비율 ≤ 허용 수준(값을 정하고 근거를 적음; 예: 5 %). 마진 m은 보정점 잔차의 상위 분위수 + 여유분, 비음수.
- 출력: 배치 (A)(B)(C)별로 **등록된 구간 목록**, 등록 실패 구간과 실패 조건, 결과 검증 영역, 각 구간의 마진 함수. 그리고 신고서 §5.6 동작 예의 후보 A(L=20)·B(L=76)·C(L=110)를 이 실측 검증 영역으로 다시 판정한 결과.

**산출물** `v1_validation_region.md` + `v1_region.json`. 구현은 `experiments/p2_evidence/v1_region.py`(플래너 코드 변경 없음 — 이 절차는 아직 `planner/`에 없으며, 넣을지는 결과를 보고 별도 결정).

**테스트** `tests/test_p2ev_region.py` — 합성 5점: (i) 비율 조건 위반 구간 미등록, (ii) 검증점 초과 비율 초과 구간 미등록, (iii) 전부 보정점이면 등록 0, (iv) 등록 구간의 합집합이 검증 영역.

### STEP V2 — 개루프 실측: 예측 동시성 vs 실측 동시성 (A40 노드, 반나절)

**목적** 청구항 2의 근거. 도착률 구동(개루프) 워크로드에서 시뮬레이터가 예측한 서빙 동시성 `L_pred`와 실측 서빙 동시성 `L_meas`가 갈라지는지, 갈라진다면 어느 좌표로 검증 정보를 조회해야 판정이 맞는지.

**왜 지금 데이터로는 안 되는가** 9점과 A40 3점은 모두 (i) closed-loop(동시성 고정) 실측 또는 (ii) 도착률을 맞춘 비교여서 `L_pred ≈ L_meas`가 구성상 보장된다. 개루프에서는 낙관적 예측기(지연을 작게 예측)가 Little's law로 `L_pred < L_meas`를 내고, 비관적 예측기는 반대다. 이 차이가 실제로 몇 %인지, 그리고 검증 정보를 `L_pred`로 조회했을 때와 `L_meas`로 조회했을 때 마진과 판정이 어떻게 달라지는지를 재야 한다.

**할 일**
- 부하 생성기: `experiments/scripts/replay_to_endpoint.py`는 도착 시각을 무시한다(docstring). `bench/core/runner.py`는 `arrival_time_ns`를 지키지만 in-process `AsyncLLM`용이다. **조사 필요**: 배포된 vLLM 서버(`planner deploy`)에 대해 `arrival_time_ns`를 지키는 개루프 드라이버가 있는지. 없으면 `replay_to_endpoint.py`에 `--open-loop`(도착 시각 준수, 동시성 무제한) 옵션을 추가한다(작은 변경, 별도 PR).
- 워크로드: `workloads/sharegpt-llama-3.1-8b-300-sps10.jsonl`의 도착률을 3~4단계로 스케일(예: 2, 5, 10, 15 rps — 서버가 포화하지 않는 범위에서 위로). 각 단계 300요청.
- 실측: `planner deploy`로 A40 TP=1(또는 기존 fixture 승자 구성) 배포 → 개루프 드라이버 → 서버 `/metrics`와 클라이언트 요청별 (도착, 첫 토큰, 완료) 시각 기록 → `L_meas = Σ체류시간 / 관측 구간`(신고서 정의; 대기 요청 포함 여부를 명시).
- 예측: 같은 트레이스(같은 도착 시각)를 LLMServingSim으로 돌려 `L_pred`(`served_concurrency_from_sim`)와 TTFT/TPOT 예측.
- 분석: 단계별 `L_pred`, `L_meas`, 차이 %, TPOT 잔차(신고서 규약). 그리고 A40 검증 정보를 (a) `L_pred`로 조회한 마진, (b) `L_meas`로 조회한 마진, 각각으로 보정한 TPOT가 실측 TPOT를 덮는지(보정 지연 ≥ 실측인 비율). **가설**: 낙관 구간에서 `L_pred < L_meas`이고, 검증 정보도 `L_pred` 좌표로 만들어졌으므로 (a)가 일관되게 덮고 (b)는 어긋난다. 반대로 나오면 그대로 기록한다 — 그 경우 청구항 2의 좌표 선택 근거를 다시 써야 한다.
- 부수 산출: 이 4단계 실측은 A40 도메인에 **개루프 점 4개**를 추가한다(`a40.accuracy.yaml`의 새 파일 `a40.accuracy.openloop.yaml`, `arrival_process: open_loop`). 기존 3점(closed/matched)과 겹치는 동시성에서 잔차가 같은지 비교 — D19(closed-loop TTFT는 큐잉으로 부풀려짐)의 실측 확인이 된다.

**산출물** `v2_openloop_concurrency.md`, `outputs/p2_evidence/v2/*.json`, `profiles/calibration/a40.accuracy.openloop.yaml`(신규).

**테스트** 개루프 드라이버: 합성 도착 시각 10개에 대해 실제 발사 시각 오차 < 20 ms(로컬 더미 서버).

### STEP V3 — 판정 정확성: 대표 후보 실제 배포 (A40 노드, 반나절~1일)

**목적** 신고서 §6이 요구한 "실제 SLO 위반 후보의 통과 비율 / 실제 만족 후보의 제외 비율 / 보류 후보의 실제 상태"를 채운다. E-A1은 규칙별 통과 **수**만 있고 정답이 없다.

**대표 후보 선정** (E-A1 fixture `pd-rngd-gpu-card`, TPOT SLO 50 ms; A40 노드에서 실행 가능한 **A40 후보만** — RNGD 후보는 이 STEP 범위 밖이며 그 사실을 기록)
- (P1) 네 규칙 모두 통과한 후보 1개 (합의 통과 — 기준선)
- (P2) 마진 없음은 통과, 동작점별 마진은 미통과인 후보 1~2개 (발명이 잡아낸 낙관 후보 — **핵심 사례**)
- (P3) 전역 18 %는 미통과, 동작점별 마진은 통과인 후보 1~2개 (발명이 구제한 후보)
- (P4) `refuse`에서 보류된 30개 중 A40 후보 1개 (보류가 정당했는지 — 실측 결과가 어느 쪽이든 "판단 근거 부족"이었음은 변하지 않으나, 실측값을 기록)
- E-A1 결과 json에서 위 조건에 맞는 후보 id와 예측 TPOT p99·동작점을 뽑아 표로 먼저 만들고(CPU), 사용자가 확인한 뒤 배포한다.

**할 일**
- 각 후보를 `planner deploy`로 배포(TP/DP/노브를 후보 그대로), 같은 워크로드를 **후보의 예측 동작점과 같은 부하로** 구동(V2의 개루프 드라이버로 도착률을 맞추거나, D32 방식으로 도착률 일치 후 서빙 동시성 확인 ±20 %), TTFT/TPOT p99 실측(서버 `/metrics` 기준, 집계 기준을 V0 결론과 일치).
- 판정 대조표: 후보 × {마진 없음, 전역 18 %, 동작점별, 동작점별+보류} 판정 vs 실측 SLO 만족 여부. 지표: 실제 위반 후보 통과율, 실제 만족 후보 제외율, 보류 후보의 실측 상태.
- 후보 수가 5~6개라 비율은 통계가 아니라 **사례**다. 명세서에는 사례 표로 쓰고 비율 일반화는 하지 않는다(신고서 §6 "보류 후보의 실제 위반 비율만으로 정당성을 판단하지 않는다"와 같은 태도).

**산출물** `v3_verdict_accuracy.md`(대표 후보 표, 판정 대조표, 각 후보의 실측 원자료 경로), `outputs/p2_evidence/v3/`.

**주의** 기존 캐시 진실(E-A1)은 시뮬레이션이고, 이 STEP의 실측이 "진짜 진실"이다. 둘이 다르면 그 차이가 곧 §6의 내용이며, 캐시를 고치지 않는다.

### STEP V4 — 도 2 실측화와 명세서 증거 정리 (CPU, 0.5일)

- V0·V1 결과로 도 2를 다시 그린다: x = 예측 서비스 동시성, 9점 잔차(신고서 규약, 부호 전환 표시), 비음수 마진 곡선(0인 구간 포함), V1에서 등록된 검증 영역(배치 B 기준), 영역 밖 "판정 보류". A40 3점(+V2 개루프 점)은 별도 패널 또는 별도 도면.
- V2 결과로 새 도면 후보: 도착률 단계별 `L_pred` vs `L_meas`와 두 좌표로 조회한 보정 TPOT의 실측 커버 여부.
- V3 결과로 새 도면 후보: 대표 후보별 예측/보정/실측 TPOT 막대(신고서 도 4의 실측판).
- `experiments/p2_evidence/results/patent2_evidence_map.md`: 신고서 v5의 각 절 ↔ 산출물 파일·수치 매핑(정규 명세서 작성 시 이 표 밖의 수치는 쓰지 않는다).

---

## 3. 일정과 자원

| STEP | 노드 | 예상 |
|---|---|---|
| V0 | CPU | 0.5일 |
| V1 | CPU | 1일 |
| V2 | A40 | 드라이버 0.5일(CPU) + 실측 반나절 |
| V3 | A40 | 후보 선정 0.5일(CPU) + 배포·실측 반나절~1일 |
| V4 | CPU | 0.5일 |

A40 노드 총 1~1.5일. RNGD 노드 불필요. 정규 명세서 작성 착수는 V3 결과 md가 나온 뒤가 적당하다(임시명세서 후 1년 기한 안에 충분).

---

## 4. 위험

| 위험 | 대응 |
|---|---|
| V1 배치 (B)에서도 등록 구간이 0 (검증점 초과 비율이 허용 수준을 넘음) | 그대로 기록. 명세서 §5.4에는 "허용 수준 x %에서 [a, b]만 등록" 또는 "등록 구간 없음 → 전부 보류"가 실시예가 된다. 허용 수준을 결과에 맞춰 조정하지 않는다 |
| V2에서 `L_pred ≈ L_meas`(차이 < 5 %) | 좌표 선택이 결과에 영향이 없다는 뜻. 청구항 2는 유지하되 명세서에서 "차이가 작은 경우 어느 좌표든 같은 판정" 서술. 부하를 포화 근처로 한 단계 더 올려 차이가 커지는지 확인(1회만) |
| V3 대표 후보가 A40에서 배포 실패(KV 할당 등, HANDOVER 2.7) | 같은 조건의 다음 후보로 대체하고 실패를 기록. 후보 선정 표에 대체 후보를 미리 둔다 |
| V3 실측이 E-A1 시뮬레이션 진실과 크게 다름 | 그것이 결과. E-A1의 판정 비교는 "규칙 간 차이"로만 인용하고 정확성은 V3만 인용 |
| 52.7/43.2의 집계 기준이 서로 다름(V0) | 같은 기준으로 재계산한 값으로 신고서·명세서 수치를 바꾸고, 도 4의 후보 B 값도 갱신 |
| 57.1이 다른 신고서·논문 초안에도 남아 있음 | V0의 출현 표를 특허 1 명세서(ETRI 내부 처리 중)에도 적용 — 특허 1 명세서에 57.1이 있으면 담당 변리사에게 정정 요청 |
